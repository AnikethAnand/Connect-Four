# Connect Four: Pop-In & Pop-Out
A C implementation of Connect Four with advanced mechanics.

## How to Compile
Navigate to the `src` folder and run:
`gcc *.c -o ../connect4.exe`

## How to Play
Enter moves using `Side Index` (e.g., `T 3` to drop from Top at index 3).
